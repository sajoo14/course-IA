### --- GENERAL --- ###
# Archivos de sistema operativo (basura de Mac y Windows)
.DS_Store
Thumbs.db

# Configuración de tu editor (VS Code)
.vscode/
.idea/

### --- BACKEND (Python) --- ###
# Carpetas de entornos virtuales (donde se instalan librerías de Python)
venv/
.venv/
env/

# Archivos compilados de Python (se generan solos al ejecutar, no sirven en git)
__pycache__/
*.pyc

# Logs de errores (he visto que tienes estos archivos)
*.log
backend/ai_debug.log
backend/ai_error.log

# Variables de entorno (SECRETOS - NUNCA SUBIR)
.env
backend/.env

### --- FRONTEND (Node/React) --- ###
# Dependencias gigantescas (se instalan con npm install)
node_modules/

# Carpeta de producción (se crea con npm run build)
dist/
build/

# Logs de npm
npm-debug.log*
yarn-debug.log*
yarn-error.log*